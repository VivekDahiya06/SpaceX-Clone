import React, { FC } from 'react'

interface MenuItem {
    name: string
    url: string
}

const Menu: FC<MenuItem[]> = (MenuItem) => {
    return (
        <section>
            <ul>
                {

                }
            </ul>
        </section>
    )
}

export default Menu
